# 共用 `<Modal>` a11y 元件 — 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 抽共用 `<Modal>`（role=dialog + Esc/點背景/X 關閉 + focus trap + 移焦還焦 + scroll lock），三個 overlay modal 遷移；含 audit #18（背景關閉 / 移除死碼 / PasskeyPrompt 背景=稍後再說）。

**Architecture:** 新增 `src/components/Modal.jsx` 擁有 overlay + dialog 卡片 + X 鈕 + 全部 a11y 副作用；LoginModal / RequestCard / PasskeyPrompt 把卡片內容當 children 傳入。業務邏輯一律不動。

**Tech Stack:** Next.js 16 + React 19（client component）+ Tailwind v4。import 別名 `@/* → src/*`。

**設計來源：** [spec](../specs/2026-06-08-modal-a11y-design.md)（§4 API、§5 行為、§6 遷移、§7 小統一）。

**全域驗證慣例（每 Task）：** `npm run lint` 維持基準 5 problems（ThemeProvider/tool[id]/dashboard 既有）不增；commit 用 Conventional + 結尾 `Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>`。build + E2E 集中 Task 5。

---

## Task 1: 新增 `src/components/Modal.jsx`

**Files:**

- Create: `src/components/Modal.jsx`

- [ ] **Step 1: 建立 Modal 元件**

完整內容（一字不差）：

```jsx
"use client";

import { useEffect, useRef } from "react";

const FOCUSABLE =
  'a[href], button:not([disabled]), input:not([disabled]), textarea:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex="-1"])';

/**
 * 共用 overlay modal：role=dialog + aria-modal、Esc / 點背景 / X 關閉、focus trap、
 * 開啟移焦 + 關閉還焦、body scroll lock。
 * @param {{
 *   onClose: () => void,
 *   children: React.ReactNode,
 *   labelledBy?: string,
 *   label?: string,
 *   className?: string,
 *   showClose?: boolean,
 *   closeOnBackdrop?: boolean,
 * }} props
 */
export default function Modal({
  onClose,
  children,
  labelledBy,
  label,
  className = "",
  showClose = true,
  closeOnBackdrop = true,
}) {
  const panelRef = useRef(null);
  const onCloseRef = useRef(onClose);
  useEffect(() => {
    onCloseRef.current = onClose;
  }, [onClose]);

  // mount-once：移焦 + scroll lock + Esc/Tab 監聽 + 卸載還焦（用 ref 讀最新 onClose，避免 churn）
  useEffect(() => {
    const panel = panelRef.current;
    const previouslyFocused = document.activeElement;

    const focusables = panel ? panel.querySelectorAll(FOCUSABLE) : [];
    if (focusables.length > 0) focusables[0].focus();
    else panel?.focus();

    const prevOverflow = document.body.style.overflow;
    document.body.style.overflow = "hidden";

    const onKeyDown = (e) => {
      if (e.key === "Escape") {
        e.stopPropagation();
        onCloseRef.current?.();
        return;
      }
      if (e.key !== "Tab" || !panel) return;
      const items = panel.querySelectorAll(FOCUSABLE);
      if (items.length === 0) {
        e.preventDefault();
        return;
      }
      const first = items[0];
      const last = items[items.length - 1];
      if (e.shiftKey && document.activeElement === first) {
        e.preventDefault();
        last.focus();
      } else if (!e.shiftKey && document.activeElement === last) {
        e.preventDefault();
        first.focus();
      }
    };
    document.addEventListener("keydown", onKeyDown);

    if (process.env.NODE_ENV !== "production" && !labelledBy && !label) {
      console.warn("Modal: 請提供 labelledBy 或 label 以利讀屏");
    }

    return () => {
      document.removeEventListener("keydown", onKeyDown);
      document.body.style.overflow = prevOverflow;
      if (typeof previouslyFocused?.focus === "function")
        previouslyFocused.focus();
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  const handleBackdrop = (e) => {
    if (closeOnBackdrop && e.target === e.currentTarget) onClose();
  };

  return (
    <div
      className="fixed inset-0 z-[200] flex items-center justify-center bg-black/40 backdrop-blur-sm p-4"
      onClick={handleBackdrop}
    >
      <div
        ref={panelRef}
        role="dialog"
        aria-modal="true"
        aria-labelledby={labelledBy}
        aria-label={label}
        tabIndex={-1}
        className={`relative bg-[var(--color-card-bg)] rounded-3xl w-full outline-none ${className}`}
      >
        {showClose && (
          <button
            type="button"
            onClick={onClose}
            aria-label="關閉"
            className="absolute -right-3 -top-3 flex h-9 w-9 items-center justify-center rounded-2xl border border-[var(--color-card-border)] bg-[var(--color-card-bg)] text-[var(--color-text-mid)] shadow-lg transition hover:text-[var(--color-text-dark)] hover:shadow-xl focus:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-clay-purple)]/40"
          >
            <svg
              viewBox="0 0 24 24"
              width="16"
              height="16"
              fill="none"
              stroke="currentColor"
              strokeWidth="2"
              strokeLinecap="round"
              aria-hidden="true"
            >
              <path d="M6 6l12 12M18 6L6 18" />
            </svg>
          </button>
        )}
        {children}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: lint**

Run: `npm run lint` → 維持 5 problems（無新增；`eslint-disable` 註解抑制 mount-once 的 exhaustive-deps）。

- [ ] **Step 3: commit**

```bash
git add src/components/Modal.jsx
git commit -m "feat(ui): 新增共用 <Modal> a11y 元件 (audit #12)

role=dialog/aria-modal、Esc/點背景/X 關閉、focus trap、移焦還焦、
scroll lock。尚未接線。

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

## Task 2: 遷移 LoginModal

**Files:**

- Modify: `src/components/LoginModal.jsx`

- [ ] **Step 1: 加 import**

在 `import { useAuth } from "@/context/AuthContext";`（第 12 行）下一行加：

```js
import Modal from "@/components/Modal";
```

- [ ] **Step 2: 換外殼（overlay+panel+X → `<Modal>`）**

old（return 開頭到 `{/* tab bar */}` 前）：

```jsx
  return (
    <div className="fixed inset-0 z-[200] flex items-center justify-center bg-black/40 backdrop-blur-sm">
      <div className="relative bg-[var(--color-card-bg)] rounded-3xl shadow-2xl border border-[var(--color-card-border)] w-full max-w-sm mx-4 p-8 flex flex-col gap-5">
        {/* 右上角凸出的圓角小卡片：關閉 */}
        <button
          type="button"
          onClick={onClose}
          aria-label="關閉"
          className="absolute -right-3 -top-3 flex h-9 w-9 items-center justify-center rounded-2xl border border-[var(--color-card-border)] bg-[var(--color-card-bg)] text-[var(--color-text-mid)] shadow-lg transition hover:text-[var(--color-text-dark)] hover:shadow-xl focus:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-clay-purple)]/40"
        >
          <svg
            viewBox="0 0 24 24"
            width="16"
            height="16"
            fill="none"
            stroke="currentColor"
            strokeWidth="2"
            strokeLinecap="round"
            aria-hidden="true"
          >
            <path d="M6 6l12 12M18 6L6 18" />
          </svg>
        </button>
        {/* tab bar */}
```

new：

```jsx
  return (
    <Modal
      onClose={onClose}
      label="登入或開發者註冊"
      className="max-w-sm p-8 flex flex-col gap-5 shadow-2xl border border-[var(--color-card-border)]"
    >
        {/* tab bar */}
```

- [ ] **Step 3: 換收尾（兩個 `</div>` → `</Modal>`）**

old（檔尾）：

```jsx
        )}
      </div>
    </div>
  );
}
```

new：

```jsx
        )}
    </Modal>
  );
}
```

- [ ] **Step 4: lint**

Run: `npm run lint` → 無新增。（prettier hook 會自動重排 children 縮排。）

- [ ] **Step 5: commit**

```bash
git add src/components/LoginModal.jsx
git commit -m "refactor(ui): LoginModal 改用共用 <Modal> (audit #12/#18)

移除自寫 overlay/X，行為邏輯不變。

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

## Task 3: 遷移 RequestCard（+ 移除死碼）

**Files:**

- Modify: `src/components/RequestCard.jsx`

- [ ] **Step 1: 加 import**

在 `import { useState } from "react";`（第 3 行）下一行加：

```js
import Modal from "@/components/Modal";
```

- [ ] **Step 2: 換外殼 + 死碼消失 + h3 加 id**

old（return 開頭到標題 div）：

```jsx
  return (
    <div className="fixed inset-0 z-[120] flex items-center justify-center bg-black/40 backdrop-blur-sm p-4">
      <div
        className="relative bg-[var(--color-card-bg)] rounded-3xl p-6 w-full max-w-2xl shadow-xl"
        onClick={(e) => e.stopPropagation()}
      >
        {/* 右上角凸出的圓角小卡片：關閉 */}
        <button
          type="button"
          onClick={onClose}
          aria-label="關閉"
          className="absolute -right-3 -top-3 flex h-9 w-9 items-center justify-center rounded-2xl border border-[var(--color-card-border)] bg-[var(--color-card-bg)] text-[var(--color-text-mid)] shadow-lg transition hover:text-[var(--color-text-dark)] hover:shadow-xl focus:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-clay-purple)]/40"
        >
          <svg
            viewBox="0 0 24 24"
            width="16"
            height="16"
            fill="none"
            stroke="currentColor"
            strokeWidth="2"
            strokeLinecap="round"
            aria-hidden="true"
          >
            <path d="M6 6l12 12M18 6L6 18" />
          </svg>
        </button>
        <div className="mb-4">
          <h3 className="font-black text-lg text-[var(--color-text-dark)]">
            提需求 / 想要的工具
          </h3>
        </div>
```

new：

```jsx
  return (
    <Modal
      onClose={onClose}
      labelledBy="req-title"
      className="max-w-2xl p-6 shadow-xl"
    >
        <div className="mb-4">
          <h3
            id="req-title"
            className="font-black text-lg text-[var(--color-text-dark)]"
          >
            提需求 / 想要的工具
          </h3>
        </div>
```

> 死碼 `onClick={(e) => e.stopPropagation()}` 隨 inner div 一起消失（Modal 用 target===currentTarget 判背景點擊，#18）。

- [ ] **Step 3: 換收尾**

old（檔尾）：

```jsx
        )}
      </div>
    </div>
  );
}
```

new：

```jsx
        )}
    </Modal>
  );
}
```

- [ ] **Step 4: lint** → 無新增。

- [ ] **Step 5: commit**

```bash
git add src/components/RequestCard.jsx
git commit -m "refactor(ui): RequestCard 改用共用 <Modal> + 移除死碼 (audit #12/#18)

刪除無效的 inner stopPropagation；點背景關閉改由 Modal 處理。

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

## Task 4: 遷移 PasskeyPrompt（背景=稍後再說、無 X）

**Files:**

- Modify: `src/components/PasskeyPrompt.jsx`

- [ ] **Step 1: 加 import**

在 `import { passkeySupported, registerPasskey, listPasskeys } from "@/lib/passkey";`（第 5 行）下一行加：

```js
import Modal from "@/components/Modal";
```

- [ ] **Step 2: 換外殼（背景/Esc=稍後再說、不顯示 X）**

old：

```jsx
  return (
    <div className="fixed inset-0 z-[210] flex items-center justify-center bg-black/40 backdrop-blur-sm px-4">
      <div className="bg-[var(--color-card-bg)] rounded-3xl shadow-2xl border border-[var(--color-card-border)] w-full max-w-sm p-8 flex flex-col gap-4 text-center">
        <div className="text-5xl">🔐</div>
```

new：

```jsx
  return (
    <Modal
      onClose={() => dismiss(false)}
      labelledBy="pk-title"
      showClose={false}
      className="max-w-sm p-8 flex flex-col gap-4 text-center shadow-2xl border border-[var(--color-card-border)]"
    >
        <div className="text-5xl">🔐</div>
```

- [ ] **Step 3: 兩個 h2 加 `id="pk-title"`（done 與未 done 分支各一，互斥不衝突）**

done 分支 old：

```jsx
<h2 className="text-xl font-black text-[var(--color-text-dark)]">設定完成！</h2>
```

new：

```jsx
<h2 id="pk-title" className="text-xl font-black text-[var(--color-text-dark)]">
  設定完成！
</h2>
```

未 done 分支 old：

```jsx
<h2 className="text-xl font-black text-[var(--color-text-dark)]">
  要不要設定 Face ID 快速登入？
</h2>
```

new：

```jsx
<h2 id="pk-title" className="text-xl font-black text-[var(--color-text-dark)]">
  要不要設定 Face ID 快速登入？
</h2>
```

- [ ] **Step 4: 換收尾**

old（檔尾）：

```jsx
        )}
      </div>
    </div>
  );
}
```

new：

```jsx
        )}
    </Modal>
  );
}
```

- [ ] **Step 5: lint** → 無新增。

- [ ] **Step 6: commit**

```bash
git add src/components/PasskeyPrompt.jsx
git commit -m "refactor(ui): PasskeyPrompt 改用共用 <Modal> (audit #12/#18)

背景/Esc = 稍後再說（不寫 localStorage）、無 X（保留兩顆 dismiss 鈕）。

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

## Task 5: build + E2E 驗證

**Files:** 無（驗證）

- [ ] **Step 1: build + lint + unit**

Run: `npm run build` → 綠。
Run: `npm run lint` → 5 problems（無新增）。
Run: `npm run test:unit` → 26 綠（未動 lib）。

- [ ] **Step 2: 起 dev server**

Run（背景）：`npm run dev`，等 `Ready`（localhost:3000）。

> 若 port 3000 殘留舊 server，先 `taskkill //PID <pid> //F`（netstat -ano | grep :3000）。

- [ ] **Step 3: playwright E2E — LoginModal**

navigate `http://localhost:3000`；點 navbar「登入👨‍💻 / 註冊🔑」開 modal。逐項驗（用 browser_evaluate 讀 `document.activeElement` / DOM 屬性、browser_press_key 送鍵）：

1. 開啟後焦點在卡片內（`document.activeElement` 在 `[role=dialog]` 內）
2. `[role=dialog][aria-modal="true"]` 存在、有 `aria-label="登入或開發者註冊"`
3. 連按 Tab 數次、焦點不離開卡片（`dialog.contains(document.activeElement)` 恆 true）
4. Shift+Tab 從第一個 focusable → 跳最後一個
5. 按 Escape → modal 消失（`[role=dialog]` 不存在）
6. 重開 → 點背景（overlay 座標，如左上角 (10,10)）→ 關閉；重開點卡片內 → 不關
7. 關閉後焦點回到「登入」觸發鈕

- [ ] **Step 4: playwright E2E — RequestCard**

navigate `http://localhost:3000`；點「💬 提需求給我」開。驗：`[role=dialog]` + `aria-labelledby="req-title"`、focus trap、Esc 關、點背景關、點內部不關、還焦。

- [ ] **Step 5: 停 dev server**

`taskkill //PID <pid> //F`（netstat :3000）。

- [ ] **Step 6: 視覺 spot-check（可選）**

light/dark 各截 LoginModal + RequestCard 開啟態，確認外觀與遷移前一致（X 凸出右上、blur 背景、置中）。

> PasskeyPrompt 需登入觸發首登提示 → **留 Jason 手動**（背景/Esc=稍後再說、兩鈕在、無 X）。

---

## 完成後

- 推 `feature-modal-a11y` → 開 PR（base main；body：spec 連結、§7 三項小統一、E2E 結果、待 Jason 手驗 PasskeyPrompt + 正式站點按、與 #23 的 LoginModal auto-merge 註記）。
- 獨立 reviewer subagent → CI/Vercel 綠 → 等 Jason merge。

---

## Self-Review（plan vs spec）

- **Spec coverage**：§3 架構→T1；§4 API→T1（7 props 全實作）；§5 行為 1-6→T1（語意/Esc/trap/移焦還焦/背景 target===currentTarget/scroll lock 皆在 Modal.jsx）；§6.1→T2、§6.2→T3（含死碼移除）、§6.3→T4（onClose=dismiss(false)/showClose=false/兩 h2 id）；§7 小統一（z-200/p-4 在 Modal overlay；alert 不動＝未列 task ✓）；§8 測試→T5。
- **Placeholder scan**：Modal.jsx 完整、各遷移 exact old→new、E2E 步驟具體；無 TBD/「同上」。✓
- **一致性**：props 名（onClose/labelledBy/label/className/showClose/closeOnBackdrop）全程一致；LoginModal 用 `label`、RequestCard/PasskeyPrompt 用 `labelledBy`+對應 id（req-title/pk-title）一致；import `@/components/Modal` 一致。✓
- **churn 防護**：onClose 走 `onCloseRef` + mount-once `[]` effect，PasskeyPrompt 傳 inline arrow 也不會每 render re-focus/re-lock。✓
