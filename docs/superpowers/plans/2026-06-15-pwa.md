# PWA（可安裝 + 離線殼）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 平台可安裝到手機主畫面（standalone + 品牌 icon），離線開啟顯示品牌離線頁。

**Architecture:** `app/manifest.js`（Next 自動 link）宣告 standalone + icons；icons 用 `next/og` 動態渲染品牌 HubMark（白 mark 於品牌漸層底，零字型 fetch、零新依賴）；最小手寫 `public/sw.js`（precache `/offline`、navigation network-first→離線回 `/offline`）由 `ServiceWorkerRegister`（production）註冊。不快取動態資料。

**Tech Stack:** Next 16 App Router（manifest 慣例 / Route Handler / ImageResponse / apple-icon 慣例）, React 19, Tailwind 4, ESM。**無 unit test**（無純邏輯；config + 瀏覽器 SW + JSX/ImageResponse）。

**Spec:** `docs/superpowers/specs/2026-06-15-pwa-design.md`

**驗證指令**

- Lint：`npm run lint`
- Build：`npm run build`（若失敗訊息只有 `fonts.gstatic.com` / "Failed to fetch" 屬本機網路 flake，重跑即可，最多 2 次）
- 語法：`node --check <file>`（.js/.mjs）

> 注意（Next 16）：dynamic Route Handler 的 `params` 是 **Promise**，需 `await params`。本計畫的 icon route 已照此寫。

---

## File Structure

- `src/lib/brandMark.mjs`（新）— 白色品牌 mark data-URI + 漸層常數（icon 共用）
- `app/manifest.js`（新）— PWA manifest
- `app/icons/[size]/route.js`（新）— 動態 PWA icon（192/512）
- `app/apple-icon.js`（新）— iOS 主畫面 icon（180）
- `app/offline/page.jsx`（新）— 品牌離線頁
- `public/sw.js`（新）— 最小 service worker
- `src/components/ServiceWorkerRegister.jsx`（新）— production 註冊 SW
- `app/layout.js`（改）— 掛 `<ServiceWorkerRegister/>`

---

## Task 1: manifest + 品牌 icon（brandMark + icons route + apple-icon + manifest）

**Files:**

- Create: `src/lib/brandMark.mjs`, `app/icons/[size]/route.js`, `app/apple-icon.js`, `app/manifest.js`

- [ ] **Step 1: brandMark 常數** — 建立 `src/lib/brandMark.mjs`：

```js
// 品牌 hub mark（白色版，置於漸層底，給 PWA icon / apple-icon 用）。
// 形狀同 src/components/HubMark.jsx / app/opengraph-image.js 的 MARK_SVG。
const WHITE_MARK_SVG = `<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" viewBox="0 0 100 100"><g fill="none" stroke="#ffffff" stroke-width="4.4" stroke-linecap="round"><path d="M50 50 Q41.9 34.3 24 26"/><path d="M50 50 Q66.6 45.4 78 30"/><path d="M50 50 Q60 66.4 80 74"/><path d="M50 50 Q33.9 59.1 26 78"/></g><g fill="#ffffff"><circle cx="50" cy="50" r="11.5"/><circle cx="24" cy="26" r="6.5"/><circle cx="78" cy="30" r="6"/><circle cx="80" cy="74" r="7"/><circle cx="26" cy="78" r="5.5"/></g></svg>`;

export const WHITE_MARK_DATA_URI = `data:image/svg+xml;base64,${Buffer.from(WHITE_MARK_SVG).toString("base64")}`;
export const BRAND_GRADIENT = "linear-gradient(135deg, #a78bfa, #6366f1)";
```

- [ ] **Step 2: icon route** — 建立 `app/icons/[size]/route.js`：

```js
import { ImageResponse } from "next/og";
import { WHITE_MARK_DATA_URI, BRAND_GRADIENT } from "@/lib/brandMark.mjs";

const ALLOWED = new Set([192, 512]);

// GET /icons/192 或 /icons/512 → 品牌 PWA icon（白 mark 於漸層底，maskable 安全區留白）。
export async function GET(request, { params }) {
  const { size: sizeParam } = await params;
  const n = Number(sizeParam);
  const size = ALLOWED.has(n) ? n : 512;
  const mark = Math.round(size * 0.64);
  return new ImageResponse(
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: BRAND_GRADIENT,
      }}
    >
      <img width={mark} height={mark} src={WHITE_MARK_DATA_URI} alt="" />
    </div>,
    { width: size, height: size },
  );
}
```

- [ ] **Step 3: apple-icon** — 建立 `app/apple-icon.js`：

```js
import { ImageResponse } from "next/og";
import { WHITE_MARK_DATA_URI, BRAND_GRADIENT } from "@/lib/brandMark.mjs";

export const size = { width: 180, height: 180 };
export const contentType = "image/png";

// iOS 加入主畫面的 icon（Next 自動加 apple-touch-icon link）。
export default function AppleIcon() {
  return new ImageResponse(
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: BRAND_GRADIENT,
      }}
    >
      <img width={115} height={115} src={WHITE_MARK_DATA_URI} alt="" />
    </div>,
    { ...size },
  );
}
```

- [ ] **Step 4: manifest** — 建立 `app/manifest.js`：

```js
// Next 16 manifest 慣例：自動注入 <link rel="manifest">。
export default function manifest() {
  return {
    name: "SimHope AI 工具中心",
    short_name: "SimHope",
    description: "公司內部 AI 資源中心 — 工具、平臺、專案、MCP、Skill",
    start_url: "/",
    display: "standalone",
    background_color: "#ffffff",
    theme_color: "#6366f1",
    icons: [
      {
        src: "/icons/192",
        sizes: "192x192",
        type: "image/png",
        purpose: "any maskable",
      },
      {
        src: "/icons/512",
        sizes: "512x512",
        type: "image/png",
        purpose: "any maskable",
      },
    ],
  };
}
```

- [ ] **Step 5: 語法檢查**

Run: `node --check src/lib/brandMark.mjs && node --check app/manifest.js && echo "OK"`
Expected: `OK`（icon route 與 apple-icon 含 JSX，由 Task 4 build 驗）

- [ ] **Step 6: Commit**

```bash
git add src/lib/brandMark.mjs "app/icons/[size]/route.js" app/apple-icon.js app/manifest.js
git commit -m "feat(pwa): manifest + 品牌 icon（next/og 動態，192/512/apple）

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

## Task 2: 離線殼（offline 頁 + service worker + 註冊元件）

**Files:**

- Create: `app/offline/page.jsx`, `public/sw.js`, `src/components/ServiceWorkerRegister.jsx`

- [ ] **Step 1: 離線頁** — 建立 `app/offline/page.jsx`：

```jsx
export const metadata = { title: "離線 · SimHope" };

// root layout 已有 <main>，這裡用 <div> 不巢狀。
export default function Offline() {
  return (
    <div className="flex flex-col items-center justify-center text-center px-6 py-24 min-h-[50vh]">
      <span className="text-6xl mb-4" aria-hidden="true">
        📡
      </span>
      <h1 className="text-3xl font-black text-[var(--color-text-dark)] mb-2">
        你目前離線
      </h1>
      <p className="text-[var(--color-text-mid)] font-semibold">
        連上網路後重新整理即可繼續使用 SimHope。
      </p>
    </div>
  );
}
```

- [ ] **Step 2: service worker** — 建立 `public/sw.js`：

```js
// SimHope PWA 最小 service worker：可安裝 + 離線殼。
// 只快取離線 fallback 頁；導覽請求 network-first，離線時回 /offline。不快取動態資料。
const CACHE = "simhope-shell-v1";
const OFFLINE_URL = "/offline";

self.addEventListener("install", (event) => {
  event.waitUntil(
    caches
      .open(CACHE)
      .then((c) => c.add(OFFLINE_URL))
      .then(() => self.skipWaiting()),
  );
});

self.addEventListener("activate", (event) => {
  event.waitUntil(
    caches
      .keys()
      .then((keys) =>
        Promise.all(
          keys.filter((k) => k !== CACHE).map((k) => caches.delete(k)),
        ),
      )
      .then(() => self.clients.claim()),
  );
});

self.addEventListener("fetch", (event) => {
  const req = event.request;
  if (req.mode !== "navigate") return; // 只接管導覽，其餘放行（不快取動態資料）
  event.respondWith(fetch(req).catch(() => caches.match(OFFLINE_URL)));
});
```

- [ ] **Step 3: 註冊元件** — 建立 `src/components/ServiceWorkerRegister.jsx`：

```jsx
"use client";

import { useEffect } from "react";

/** Production 時註冊 /sw.js（PWA 離線殼）。無 UI。dev 不註冊，避免快取干擾開發。 */
export default function ServiceWorkerRegister() {
  useEffect(() => {
    if (
      process.env.NODE_ENV === "production" &&
      typeof navigator !== "undefined" &&
      "serviceWorker" in navigator
    ) {
      navigator.serviceWorker.register("/sw.js").catch((e) => {
        console.error("SW 註冊失敗:", e);
      });
    }
  }, []);
  return null;
}
```

- [ ] **Step 4: 語法檢查 + Lint**

Run: `node --check public/sw.js && echo "sw ok"`
Expected: `sw ok`

Run: `npm run lint`
Expected: 0 errors（2 既有 `<img>` warning）

- [ ] **Step 5: Commit**

```bash
git add app/offline/page.jsx public/sw.js src/components/ServiceWorkerRegister.jsx
git commit -m "feat(pwa): 離線殼（offline 頁 + 最小 service worker + 註冊元件）

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

## Task 3: layout 掛載 ServiceWorkerRegister

**Files:**

- Modify: `app/layout.js`（import + body）

- [ ] **Step 1: 加 import** — 在 `app/layout.js`，於 `import PasskeyPrompt from "@/components/PasskeyPrompt";`（第 9 行）之後加一行：

```js
import ServiceWorkerRegister from "@/components/ServiceWorkerRegister";
```

- [ ] **Step 2: 掛元件** — 找到 body 內的 FOUC 主題 `<script .../>`（`dangerouslySetInnerHTML` 的那段，緊接在 `<body ...>` 之後）。在那個 `<script ... />` 之後、`{/* a11y: 跳到主內容 ... */}` 的 skip-link `<a>` 之前，插入：

```jsx
<ServiceWorkerRegister />
```

- [ ] **Step 3: Lint**

Run: `npm run lint`
Expected: 0 errors

- [ ] **Step 4: Commit**

```bash
git add app/layout.js
git commit -m "feat(pwa): layout 掛載 ServiceWorkerRegister

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

## Task 4: 全量驗證

- [ ] **Step 1: 單元測試（確認沒弄壞既有）**

Run: `npm run test:unit`
Expected: 0 fail（本功能未加測；確認既有測試不受影響）

- [ ] **Step 2: Lint**

Run: `npm run lint`
Expected: 0 errors（2 既有 `<img>` warning）

- [ ] **Step 3: Build**

Run: `npm run build`
Expected: `✓ Compiled successfully`，且 route 清單出現 `/manifest.webmanifest`、`/icons/[size]`、`/apple-icon`、`/offline`。

- [ ] **Step 4: 收尾** — `git status` 乾淨；推 branch、開 PR。PR 標註：**零新付費 API / 零新依賴 / 無 rules/資料/SA → merge 即部署**；**無 unit test（無純邏輯）**。部署後人工驗：Lighthouse PWA、手機加入主畫面（standalone + 品牌 icon）、裝好後離線開啟看品牌離線頁、`/icons/192`+`/icons/512`+`/apple-icon` 回正常 PNG。

---

## 範圍外（路線圖）

push 通知、完整離線資料快取、背景同步。
