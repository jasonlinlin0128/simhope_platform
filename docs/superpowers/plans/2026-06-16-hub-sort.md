# /hub 排序（最熱門 + 最新）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** /hub 資源中心加 sort control「🔥 最熱門（全期 views，預設）/ 🆕 最新（createdAt）」，把使用訊號帶進主瀏覽面並讓新工具浮上來。

**Architecture:** 新增純函式 `sortTools(tools, mode, viewsMap)`（client-side 排序，TDD）。`app/hub/page.jsx`（server）多抓 `getServerToolViews()` 把 `viewsMap` 當 prop 傳給 `HubExplorer`；`HubExplorer`（client island）加 `sortMode` state + toggle UI，在既有 filtered pipeline 末端套 `sortTools`（搜尋中維持 Fuse 相關度）。

**Tech Stack:** Next.js 16 (App Router, RSC) · React 19 · 純 ESM JavaScript · node:test · Tailwind 4 · Fuse.js（既有）。

**Spec:** `docs/superpowers/specs/2026-06-16-hub-sort-design.md`

---

## File Structure

| 檔案                             | 動作 | 責任                                                                         |
| -------------------------------- | ---- | ---------------------------------------------------------------------------- |
| `src/lib/sortTools.mjs`          | 新增 | 純函式 `sortTools(tools, mode, viewsMap)`（popular/recent，穩定、不 mutate） |
| `src/lib/sortTools.test.mjs`     | 新增 | node:test                                                                    |
| `app/hub/page.jsx`               | 改   | 多抓 `getServerToolViews()`、傳 `viewsMap` prop                              |
| `src/components/HubExplorer.jsx` | 改   | `viewsMap` prop + `sortMode` state + toggle UI + pipeline 套 sort            |

**不需改**：`ToolCard.jsx`、`app/page.jsx`、`src/lib/serverCatalog.js`（`getServerToolViews` 已存在）。

---

### Task 1: 純函式 `sortTools` + TDD

**Files:**

- Create: `src/lib/sortTools.mjs`
- Test: `src/lib/sortTools.test.mjs`

- [ ] **Step 1: 先寫失敗的測試** — 建立 `src/lib/sortTools.test.mjs`：

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { sortTools } from "./sortTools.mjs";

const T = (id, extra = {}) => ({ id, title: id, ...extra });

test("popular：依 viewsMap 由大到小", () => {
  const out = sortTools([T("a"), T("b"), T("c")], "popular", {
    a: 5,
    b: 20,
    c: 12,
  });
  assert.deepEqual(
    out.map((t) => t.id),
    ["b", "c", "a"],
  );
});

test("popular：缺 key / 0 view 視為 0 沉底，且穩定保持原序", () => {
  const out = sortTools([T("a"), T("b"), T("c"), T("d")], "popular", { c: 7 });
  // c(7) 在前；a,b,d 同為 0 → 維持原順序
  assert.deepEqual(
    out.map((t) => t.id),
    ["c", "a", "b", "d"],
  );
});

test("popular：viewsMap undefined 安全（全 0、原序）", () => {
  const out = sortTools([T("a"), T("b")], "popular");
  assert.deepEqual(
    out.map((t) => t.id),
    ["a", "b"],
  );
});

test("recent：依 createdAt(ISO) 新到舊", () => {
  const tools = [
    T("old", { createdAt: "2026-01-01T00:00:00Z" }),
    T("new", { createdAt: "2026-06-01T00:00:00Z" }),
    T("mid", { createdAt: "2026-03-01T00:00:00Z" }),
  ];
  const out = sortTools(tools, "recent");
  assert.deepEqual(
    out.map((t) => t.id),
    ["new", "mid", "old"],
  );
});

test("recent：缺 / 無法解析 createdAt → 排最後（維持原相對順序）", () => {
  const tools = [
    T("none"),
    T("dated", { createdAt: "2026-05-01T00:00:00Z" }),
    T("bad", { createdAt: "not-a-date" }),
  ];
  const out = sortTools(tools, "recent");
  assert.equal(out[0].id, "dated");
  assert.deepEqual(
    out.slice(1).map((t) => t.id),
    ["none", "bad"],
  );
});

test("未知 mode → 回原序（淺拷貝、新陣列）", () => {
  const tools = [T("a"), T("b"), T("c")];
  const out = sortTools(tools, "xxx", { a: 1, b: 99 });
  assert.deepEqual(
    out.map((t) => t.id),
    ["a", "b", "c"],
  );
  assert.notEqual(out, tools);
});

test("tools 非陣列 / undefined → []", () => {
  assert.deepEqual(sortTools(undefined, "popular"), []);
  assert.deepEqual(sortTools(null, "recent"), []);
  assert.deepEqual(sortTools("nope", "popular"), []);
});

test("不 mutate 原 tools 陣列", () => {
  const tools = [T("a"), T("b"), T("c")];
  const before = tools.map((t) => t.id);
  sortTools(tools, "popular", { c: 9 });
  assert.deepEqual(
    tools.map((t) => t.id),
    before,
  );
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `node --test src/lib/sortTools.test.mjs`
Expected: FAIL — `Cannot find module './sortTools.mjs'`。

- [ ] **Step 3: 寫最小實作** — 建立 `src/lib/sortTools.mjs`：

```js
// src/lib/sortTools.mjs
// 純函式：依排序模式排 /hub 工具清單。無 firebase/browser 依賴，可 node:test。

/**
 * 把工具清單依 mode 排序（回新陣列、不 mutate、穩定排序）。
 * @param {object[]} tools
 * @param {"popular"|"recent"} mode
 * @param {Record<string, number>} [viewsMap] { toolId: 全期瀏覽數 }（popular 用）
 * @returns {object[]}
 */
export function sortTools(tools, mode, viewsMap = {}) {
  if (!Array.isArray(tools)) return [];
  const arr = tools.slice(); // 淺拷貝，不 mutate 原陣列；V8 sort 為穩定排序
  if (mode === "popular") {
    const views = (t) => Number(viewsMap?.[t?.id]) || 0;
    return arr.sort((a, b) => views(b) - views(a)); // 同分 → 0 → 維持原序
  }
  if (mode === "recent") {
    const ms = (t) => {
      const n = Date.parse(t?.createdAt);
      return Number.isNaN(n) ? -Infinity : n; // 缺/壞 createdAt → 視為最舊
    };
    // 顯式比較避免 (-Infinity)-(-Infinity)=NaN 的比較器 UB
    return arr.sort((a, b) => {
      const x = ms(a);
      const y = ms(b);
      return x === y ? 0 : y > x ? 1 : -1;
    });
  }
  return arr; // 未知 mode → 原序淺拷貝（安全預設）
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `node --test src/lib/sortTools.test.mjs`
Expected: PASS — 8 個 test 全綠。

- [ ] **Step 5: Commit**

```bash
git add src/lib/sortTools.mjs src/lib/sortTools.test.mjs
git commit -m "feat(hub-sort): sortTools 純函式（popular/recent，穩定不 mutate）+ TDD

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

### Task 2: hub/page.jsx 多抓 views、傳 viewsMap

**Files:**

- Modify: `app/hub/page.jsx`

先 READ 確認現況：該檔已 import `getServerCatalog, getServerToolHelpful` 並 `attachHelpfulCounts`，`Promise.all` destructure `[tools, toolHelpful]`，回 `<HubExplorer tools={enriched} initialCat={initialCat} />`。

- [ ] **Step 1: 整檔替換為**：

```jsx
import {
  getServerCatalog,
  getServerToolHelpful,
  getServerToolViews,
} from "@/lib/serverCatalog";
import { attachHelpfulCounts } from "@/lib/helpfulBadge.mjs";
import HubExplorer from "@/components/HubExplorer";

/**
 * 資源中心（公開頁）。server 端抓 catalog → 傳 client 島；讀 ?cat= 帶初始分類
 * （故為 dynamic render，但 REST fetch 有 300s 快取）。
 */
export default async function HubPage({ searchParams }) {
  const { cat } = await searchParams;
  const [tools, toolHelpful, toolViews] = await Promise.all([
    getServerCatalog(),
    getServerToolHelpful(),
    getServerToolViews(),
  ]);
  const enriched = attachHelpfulCounts(tools, toolHelpful);
  // ?cat= 重複時 Next 給陣列；取首值，與舊 useSearchParams().get() 行為一致。
  const initialCat = (Array.isArray(cat) ? cat[0] : cat) || "all";
  return (
    <HubExplorer
      tools={enriched}
      viewsMap={toolViews}
      initialCat={initialCat}
    />
  );
}
```

- [ ] **Step 2: lint 不破**

Run: `npm run lint`
Expected: 0 error（既有 2 img warning 不算）。

- [ ] **Step 3: Commit**

```bash
git add app/hub/page.jsx
git commit -m "feat(hub-sort): hub/page 多抓 getServerToolViews 並傳 viewsMap

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

### Task 3: HubExplorer 加 sortMode + toggle + pipeline 套 sort

**Files:**

- Modify: `src/components/HubExplorer.jsx`

先 READ 整檔確認現況（`"use client"`、imports、`HubExplorer({ tools, initialCat = "all" })`、`activeCat`/`searchQuery`/`debouncedQuery` state、`activeTools`/`counts`/`fuse`/`filtered` useMemo、search input、`<CategoryTabs>`、grid）。

- [ ] **Step 1: import `sortTools`** — 在頂部 import 區（`import { track } from "@/lib/track";` 之後）加：

```js
import { sortTools } from "@/lib/sortTools.mjs";
```

- [ ] **Step 2: 加 `viewsMap` prop** — 把函式簽名

```js
export default function HubExplorer({ tools, initialCat = "all" }) {
```

改成：

```js
export default function HubExplorer({
  tools,
  initialCat = "all",
  viewsMap = {},
}) {
```

- [ ] **Step 3: 加 `sortMode` state** — 在 `const [debouncedQuery, setDebouncedQuery] = useState("");` 之後加：

```js
const [sortMode, setSortMode] = useState("popular"); // 預設最熱門
```

- [ ] **Step 4: pipeline 末端套 sort** — 把現有 `filtered` useMemo：

```js
const filtered = useMemo(() => {
  let result = activeTools;
  if (debouncedQuery) result = fuse.search(debouncedQuery).map((r) => r.item);
  if (activeCat !== "all")
    result = result.filter(
      (t) =>
        (CATEGORY_ORDER.includes(t.category) ? t.category : "tool") ===
        activeCat,
    );
  return result;
}, [activeTools, debouncedQuery, fuse, activeCat]);
```

改成（末端依「搜尋中?」決定相關度 or sortTools，並把 `sortMode`/`viewsMap` 加進 deps）：

```js
const filtered = useMemo(() => {
  let result = activeTools;
  if (debouncedQuery) result = fuse.search(debouncedQuery).map((r) => r.item);
  if (activeCat !== "all")
    result = result.filter(
      (t) =>
        (CATEGORY_ORDER.includes(t.category) ? t.category : "tool") ===
        activeCat,
    );
  // 搜尋中維持 Fuse 相關度；否則依所選 sort（最熱門/最新）
  return debouncedQuery ? result : sortTools(result, sortMode, viewsMap);
}, [activeTools, debouncedQuery, fuse, activeCat, sortMode, viewsMap]);
```

- [ ] **Step 5: 加 sort toggle UI** — 在 `<CategoryTabs .../>` 區塊**之後**、grid `<div className="grid ...">` **之前**插入：

```jsx
<div className="flex justify-end mb-5">
  <div
    className="inline-flex gap-1 p-1 rounded-xl border border-[var(--color-card-border)] bg-white dark:bg-gray-800"
    role="group"
    aria-label="排序方式"
  >
    {[
      { key: "popular", label: "🔥 最熱門" },
      { key: "recent", label: "🆕 最新" },
    ].map((opt) => (
      <button
        key={opt.key}
        type="button"
        onClick={() => setSortMode(opt.key)}
        aria-pressed={sortMode === opt.key}
        className={`px-3 py-1.5 rounded-lg text-sm font-bold transition ${
          sortMode === opt.key
            ? "bg-[var(--color-clay-purple)] text-white"
            : "text-[var(--color-text-mid)] hover:text-[var(--color-text-dark)]"
        }`}
      >
        {opt.label}
      </button>
    ))}
  </div>
</div>
```

- [ ] **Step 6: 全套驗證**

Run: `npm run test:unit`
Expected: PASS — 既有測試 + 新 8 個 sortTools 測試全綠。

Run: `npm run lint`
Expected: 0 error（既有 2 img warning 不算）。

Run: `npm run build`
Expected: build 成功；`/hub`（ƒ Dynamic）正常生成。

> ⚠️ 本機 build 偶遇 `fonts.gstatic.com` flake（與本碼無關）；**僅當**錯誤是 Google Fonts fetch 時重試一次，其他錯誤不重試、回報。

- [ ] **Step 7: Commit**

```bash
git add src/components/HubExplorer.jsx
git commit -m "feat(hub-sort): HubExplorer 加最熱門/最新 sort toggle（搜尋中維持相關度）

Co-Authored-By: Jason simhope ai agent <jasonlin@simhope.com.tw>"
```

---

## 完成後（plan 外，主 session 處理）

- 派獨立 reviewer 對照 codebase 端到端檢查。
- 開 PR（branch `feature-hub-sort`）。
- 部署後人工驗（spec「Rollout」）。

---

## Self-Review（plan 對照 spec）

**1. Spec coverage**

- 最熱門（views，預設）+ 最新（createdAt）→ `sortTools`（Task 1）+ HubExplorer toggle 預設 `"popular"`（Task 3 Step 3/5）✓
- 搜尋中維持相關度 → Task 3 Step 4 `debouncedQuery ? result : sortTools(...)` ✓
- viewsMap 來源 → Task 2（`getServerToolViews` 既有）✓
- 不做 30 天趨勢 / URL 持久化 / 動首頁 → 無此類 task ✓
- 純函式 TDD → Task 1 ✓
- 零 rules/依賴/付費/index → 全程無 ✓

**2. Placeholder scan** — 無 TBD/TODO；每個 code step 都有完整程式碼。✓

**3. Type consistency** — `sortTools(tools, mode, viewsMap)`、`sortMode`（"popular"/"recent"）、`viewsMap` prop 跨 Task 命名一致；`setSortMode` 對應 state。✓
